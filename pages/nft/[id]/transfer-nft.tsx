 import {useEffect, useState} from "react";
 import { useRouter } from "next/router";
 const TransferNFT = (): JSX.Element => {
     const router = useRouter();
      // get nft data from the route
      // nice card with the image of the nft
      // input of the address
      // button to confirm transfer
      // transfer nft
      // feedback to the UI 
     return(<>
       <div>Transfer NFT page</div>
      </>
     )
 };
 
 export default TransferNFT;
 